package com.esp32homecontroller.app

import io.flutter.embedding.android.FlutterActivity

/**
 * MainActivity
 * ============================================================================
 * Standard single-Activity Flutter entry point. All app logic lives in Dart;
 * this class intentionally stays minimal. flutter_bluetooth_serial and
 * permission_handler register themselves automatically via Flutter's plugin
 * auto-registration, so no manual plugin registration is required here.
 * ============================================================================
 */
class MainActivity : FlutterActivity()
